// fichero 726 -- macros y constantes -- MODIFICADO
#define LIMITE_726 926
#define FACTOR_726 3

int aplicar_limite726(int valor) {
    if (valor > LIMITE_726) return LIMITE_726;
    return valor * FACTOR_726;
}
